\section{Introduction}

A medicinal chemist is shown a graph neural network's prediction that a
candidate is hERG-active, together with a heat map over its atoms. The heat map
concentrates on a piperidine nitrogen; the chemist finds that plausible, and the
series is deprioritised. Nothing in that workflow ever established that the
highlighted atoms are the atoms the model actually used, that they are the atoms
that matter chemically, or that the highlighting behaves the same way on the
next scaffold. Each of those is a separate question, and the answers come apart.

They come apart in a way that is measurable, and that gets worse in exactly the
setting drug discovery operates in. An attribution can be \emph{faithful}, in
that the atoms it ranks highest really are the atoms whose removal moves the
model's output most, and simultaneously \emph{wrong}, in that those atoms are
not the substructure that determines the property. Across the \nGtCells{}
committed cells in this study that carry a node-level ground truth,
\nFaithfulButWrong{} are positively faithful to the model and yet
\emph{anti}-aligned with the true motif (ground-truth AUROC below the chance
value of $0.5$). A benchmark that scores only faithfulness rates all of them as
fine.

In practice the problem takes the form of a selection problem. With no ground
truth available, which is the situation on every real molecular dataset in this
study, an attributor has to be chosen using a faithfulness or fidelity metric.
On the two cell families that do have a ground truth and were audited on both
splits, we can check whether that choice is the right one while changing
\emph{only} the split. Under a Bemis--Murcko scaffold split on MUTAG, all
$\mutShiftNMetrics{}$ ranking metrics (our occlusion measure, Fidelity+ and the
GraphFramEx characterisation score alike) select \mutShiftPickOccspearman{},
whose agreement with the mutagenic nitro motif is GT AUROC
$\mutShiftPickGtOccspearman{}$, while the ground truth ranks \mutShiftGtBest{}
best at $\mutShiftGtBestVal{}$. The median per-molecule gap is
$\mutShiftGapOccspearman{}$ AUROC (paired Wilcoxon
$p\mutShiftPOccspearman{}$, $n=\mutShiftNPairedOccspearman{}$; Benjamini--Hochberg
$q\mutShiftQOccspearman{}$ across the $\nSelectionTests{}$ selection tests), and
the rank correlation between faithfulness and correctness is \emph{negative}
($\mutShiftRhoOccspearman{}$ to $\mutShiftRhoCharacterization{}$). In
distribution on the same dataset, two of the three metrics instead select the
ground-truth-best attributor and the rank correlation is
$+\mutIndRhoOccspearman{}$.

None of this makes faithfulness metrics wrong. They answer a different question
from the one a chemist is asking, and the gap between the two questions widens
under distribution shift. Molecular property prediction is deployed under
scaffold shift almost by construction, since the point of a screening model is
to score series the training set does not contain, and scaffold-split
evaluation is now standard practice for the \emph{predictions}
themselves~\cite{mjepa,moleculenet}. The evaluation protocol for the
\emph{explanations} has not followed: it does not stratify by that shift at all.

\paragraph{Contributions} This paper presents \textbf{MolSanity}, a
reliability-audit framework for feature attributions on molecular GNNs. Rather
than propose another attribution method, it wraps the canonical
implementations (Captum~\cite{captum}, PyTorch Geometric~\cite{pyg},
RDKit~\cite{rdkit}) and audits them. Specifically:

\begin{itemize}\itemsep2pt
\item We define a six-axis audit over a motif-native (RDKit) decomposition:
  coherence, occlusion--attribution faithfulness, ground-truth localisation,
  cross-checkpoint stability, calibration linkage and confidence/correctness
  regime stratification, with field-standard Fidelity$\pm$, sparsity, the
  GraphFramEx characterisation score~\cite{graphframex} and the PyG/DIG
  unfaithfulness metric~\cite{dig} computed on the \emph{same} molecules for
  direct comparability (\nameref{sec:framework}).
\item We show that a faithfulness-only ranking picks the wrong attributor under
  scaffold shift, in a design where only the split varies, so the effect is not
  confounded with a change of dataset (\nameref{sec:selection}).
\item We show reliability is a property of the cell, not of the attributor: the
  backbone ordering does not survive the change of split
  ($\rho=\bbRankRho{}$ over $\bbRankN{}$ backbones on the exact-ground-truth
  set), and neither does the attributor ordering
  ($\rho=\mutAttrRankRho{}$ on MUTAG, $\synAttrRankRho{}$ on
  \synDataset{}). Faithfulness itself does \emph{not} shift systematically
  (across the $\nSplitPairs{}$ cells audited on both splits the median change
  is $\medSplitOccDelta{}$), so what shift breaks is the \emph{relationship}
  between faithfulness and correctness, not the level of faithfulness.
\item Using \nRecords{} per-molecule audit records, we
  stratify reliability by confidence/correctness regime and test the
  calibration-linkage hypothesis directly. It holds per cell
  (median $\rho=\medCalibRho{}$, $\nCalibSigPos{}$ cells significantly positive
  vs.\ $\nCalibSigNeg{}$ negative) but \emph{reverses} when cells are pooled
  ($\pooledCalibRho{}$), a Simpson's-paradox trap for anyone reporting a single
  aggregate number.
\item We report the negatives with the positives: cells whose ground truth is
  degenerate, a dataset that trains but yields no usable node labels, rows
  carried over from an earlier run (labelled, never merged into a this-run
  aggregate), and the \nNoGtCells{} cells where the correctness axis simply
  does not exist and is marked as such rather than scored.
\end{itemize}

\section{Related work}

\paragraph{Attribution methods} The attributors audited here are the standard
ones. The gradient family (Saliency~\cite{saliency}, Input×Gradient, Guided
Backpropagation~\cite{guidedbp} and Integrated Gradients~\cite{ig}) is
model-agnostic and cheap. Graph-specific methods learn a mask over the
input: GNNExplainer~\cite{gnnexplainer} optimises a soft edge/feature mask per
instance, PGExplainer~\cite{pgexplainer} amortises that into a parametric mask
network, and SubgraphX~\cite{subgraphx} searches connected subgraphs with
Monte-Carlo tree search under a Shapley objective. Graph analogues of
CAM/Grad-CAM were introduced by Pope et al.~\cite{gradcamgraph}; Yuan et
al.~\cite{gnnsurvey} survey the space. In every case we call the published
implementation rather than reimplementing it.

\paragraph{Evaluating explanations} Two evaluation traditions exist.
\emph{Faithfulness} measures whether the explanation tracks the model:
Fidelity$\pm$ and sparsity~\cite{gnnsurvey}, the GraphFramEx characterisation
score that combines them~\cite{graphframex}, and the unfaithfulness metric
shipped with DIG/PyG~\cite{dig}. \emph{Correctness} measures whether the
explanation recovers a known rationale, which requires ground-truth
substructures, typically synthetic~\cite{graphxai}. The tension between the
two is known: Faber et al.~\cite{faber} argue that comparing against ground
truth is misleading when the trained model does not use that rationale, and
Sanchez-Lengeling et al.~\cite{sanchezlengeling} built the first systematic
attribution benchmark for molecular GNNs on exactly this basis. Outside graphs,
sanity checks~\cite{sanitychecks} and retraining-based benchmarks~\cite{roar}
made a parallel case that plausible-looking attributions can be uninformative.
MolSanity's position is that neither axis subsumes the other, and that what
needs measuring is the \emph{joint} behaviour of the two under distribution
shift.

\paragraph{Frameworks, and the explicit delta} Four lines of work are close
enough that a citation is not a sufficient answer. Table~\ref{tab:related}
scores all of them on the same capabilities; what follows states what each row
of that table means.

\emph{Sanchez-Lengeling et al.}~\cite{sanchezlengeling} is the nearest
neighbour and the earliest. It built the first systematic attribution benchmark
for molecular GNNs on tasks whose ground-truth substructure is known by
construction, establishing that attribution methods should be scored against a
known rationale rather than against how plausible a chemist finds the
highlighting. MolSanity inherits that premise. What it adds is the faithfulness
axis measured on the \emph{same} molecules and the joint behaviour of the two.
A benchmark that reports correctness alone cannot show that a faithfulness-based
choice of attributor is the wrong choice, because it never makes that choice.
The selection-consistency test of \nameref{sec:selectiondef} makes it, and its
outcome changes with the split.

\emph{MolFaith}~\cite{molfaith} is the closest work on the faithfulness axis
and is larger than this study on that axis: eight attribution methods, two
molecular representations and five architectures over roughly $14{,}000$
molecules, against the $\nRecords{}$ per-molecule records here. We make no
claim to broader faithfulness coverage. Two of its findings bear directly on
ours. First, it reports that some methods are inherently more faithful than
others largely independently of architecture. We find the same stability from
the other direction: faithfulness does not shift systematically across the
$\nSplitPairs{}$ cells audited on both splits (median change
$\medSplitOccDelta{}$). Two independent studies at different scales agreeing
that faithfulness is a comparatively stable property of the method is the
premise our result then acts on, because the ordering that does \emph{not}
survive the change of split is the ground-truth one
($\rho=\mutAttrRankRho{}$ on MUTAG, $\synAttrRankRho{}$ on \synDataset{}).
A stable faithfulness ranking is therefore not evidence of a stable correctness
ranking, which is what a practitioner with no rationale labels has to assume
when they use faithfulness to choose a method. Second, it reports that
faithfulness varies markedly per molecule; our regime stratification locates
part of that variation, since ground-truth localisation collapses below chance
precisely on confidently-wrong predictions while faithfulness on the same
molecules looks unremarkable.

\emph{GraphXAI}~\cite{graphxai} and \emph{GraphFramEx}~\cite{graphframex} are
general-graph frameworks: GraphXAI supplies generators with exact node ground
truth and an evaluation library, GraphFramEx systematises fidelity-style
evaluation across explainers and tasks. Both are broader than MolSanity across
graph domains and narrower within chemistry. MolSanity scores chemically
meaningful units (SSSR rings, Murcko scaffold, BRICS fragments) rather than
individual atoms, and stratifies by the split that governs molecular
deployment. \emph{DIG}~\cite{dig} is a library rather than a protocol; we wrap
its unfaithfulness metric, and its SubgraphX implementation is the coverage gap
recorded in \nameref{sec:limits}.

\emph{What is new here, and what is not.} Ground-truth localisation,
fidelity metrics, motif decompositions and scaffold splits all exist already.
Two rows of Table~\ref{tab:related} we do not find in any of the four:
cross-checkpoint stability and calibration linkage. The contribution we would
defend is neither of those rows alone, but the result they were built to
support, that the two evaluation traditions above invert under the shift
molecular models are deployed into and that no metric on the faithfulness side
signals it.

\input{generated/tab_related}

\section{The MolSanity audit}
\label{sec:framework}

\subsection{Notation}

A molecule is a graph $G=(V,E)$ with node features $X\in\mathbb{R}^{|V|\times d}$
and edge features $Z\in\mathbb{R}^{|E|\times d'}$. A trained model
$f_\theta$ maps $G$ to logits $f_\theta(G)\in\mathbb{R}^{C}$ (classification) or
a scalar (regression); $t$ indexes the explained output. An \emph{attributor}
is a map $A:(f_\theta,G,t)\mapsto a\in\mathbb{R}^{|V|}$ assigning a real score
to every atom.

RDKit decomposes $G$ into a motif cover
$\mathcal{M}(G)=\{M_1,\dots,M_K\}$, $M_k\subseteq V$, from SSSR rings, the
Bemis--Murcko scaffold~\cite{murcko} and BRICS-style fragments. Writing
$a^+_v=\max(a_v,0)$, the motif-level score is
\begin{equation}
  s_k \;=\; \sum_{v\in M_k} a^+_v ,
  \qquad k=1,\dots,K .
  \label{eq:motifscore}
\end{equation}
Working at motif rather than atom granularity is what makes the audit
chemically legible: a chemist reasons about a nitro group, not about atom 14.

For $S\subseteq V$ let $G\!\setminus\!S$ denote $G$ with the features of $S$
zeroed through the model's node-mask multiplier,
$X' = X\odot(\mathbf{1}-\mathbb{1}_S)$, leaving the topology intact.

\subsection{The five audit statistics}

\paragraph{(i) Faithfulness} Occlusion of motif $M_k$ changes the explained
output by
\begin{equation}
  \Delta_k \;=\; f_{\theta,t}(G)\;-\;f_{\theta,t}(G\!\setminus\! M_k).
  \label{eq:occ}
\end{equation}
An attribution is faithful when it ranks motifs as occlusion does, measured by
the Spearman rank correlation over the $K$ motifs of one molecule,
\begin{equation}
  \rho_{\mathrm{occ}}(G) \;=\; \operatorname{Spearman}\!\big(
    (s_k)_{k\le K},\,(\Delta_k)_{k\le K}\big) \in[-1,1].
  \label{eq:rhoocc}
\end{equation}
$\rho_{\mathrm{occ}}=1$ means the attributor recovers the causal ordering
exactly; $\rho_{\mathrm{occ}}=0$ means it is uninformative about it; and
$\rho_{\mathrm{occ}}<0$ means it \emph{inverts} it, which is strictly worse
than an uninformative explanation. Undefined cases ($K<2$, or zero variance in
either argument) are recorded as missing, never as $0$.

We also reproduce the field-standard fidelity pair~\cite{gnnsurvey}. With
$\tau$ the $80$th percentile of $a^+$ and $S_\tau=\{v: a^+_v\ge\tau\}$ the
salient set, and $p_t(\cdot)$ the softmax probability of the explained class,
\begin{align}
  \mathrm{Fid}^{+} &= p_t(G) - p_t(G\!\setminus\!S_\tau), \label{eq:fidplus}\\
  \mathrm{Fid}^{-} &= p_t(G) - p_t(G\!\setminus\!\bar{S_\tau}), \label{eq:fidminus}
\end{align}
higher $\mathrm{Fid}^{+}$ and lower $\mathrm{Fid}^{-}$ being better, with
sparsity $1-|S_\tau|/|V|$. The GraphFramEx characterisation
score~\cite{graphframex} combines them as the weighted harmonic mean
\begin{equation}
  \mathrm{char} \;=\;
  \frac{(w_{+}+w_{-})\,\mathrm{Fid}^{+}\,(1-\mathrm{Fid}^{-})}
       {w_{-}\mathrm{Fid}^{+} + w_{+}(1-\mathrm{Fid}^{-})},
  \qquad w_{+}=w_{-}=\tfrac12 ,
  \label{eq:char}
\end{equation}
undefined outside $\mathrm{Fid}^{\pm}\in[0,1]$. That constraint matters for
the regression cells, where $\Delta$ is taken in output space and
$\mathrm{Fid}^{\pm}$ leaves that range.

\paragraph{(ii) Correctness} Where a node-level ground-truth mask
$g\in\{0,1\}^{|V|}$ exists, correctness is the ability of $a$ to separate the
true substructure from the rest, scored by the area under the ROC curve of the
min--max normalised attribution against $g$,
\begin{equation}
  \begin{aligned}
    \mathrm{GT\text{-}AUROC}(G) =\;
      &\Pr\big[\tilde a_u > \tilde a_v \,\big|\, g_u{=}1,\, g_v{=}0\big]\\[-1pt]
      &+\tfrac12\Pr\big[\tilde a_u = \tilde a_v\big],
  \end{aligned}
  \label{eq:gtauroc}
\end{equation}
with $u,v$ drawn uniformly from the positive and negative atoms. Chance is
$\tfrac12$; values below $\tfrac12$ mean the attribution is \emph{anti-aligned},
ranking the true motif systematically \emph{below} the rest. When $g$ is
degenerate ($\sum_v g_v\in\{0,|V|\}$) the quantity is undefined and reported as
such.

\paragraph{(iii) Coherence} Concentration and connectedness of the attribution
mass: the Gini coefficient of $a^+$, the fraction of mass in the top $20\%$ of
atoms, the largest-connected-component fraction of $S_\tau$ in the induced
subgraph, and the motif top-1 share $\max_k s_k/\sum_k s_k$. These are
descriptive; \nameref{sec:crosstask} shows they carry little reliability
signal.

\paragraph{(iv) Stability} With $\theta^{\mathrm{early}}$ an intermediate
checkpoint of the same training run and $s^{\mathrm{early}}$, $s^{\mathrm{final}}$
the corresponding motif scores~\eqref{eq:motifscore},
\begin{equation}
  \mathrm{stab}(G) \;=\;
  \operatorname{Spearman}\big(s^{\mathrm{early}},\,s^{\mathrm{final}}\big).
  \label{eq:stab}
\end{equation}
An explanation that reorders motifs between two checkpoints of one run is a
weak basis for a chemistry decision.

\paragraph{(v) Regime and calibration} Logits are temperature-scaled on the
validation fold~\cite{calibration}; let $c(G)=\max_j p_j(G)$ be the calibrated
confidence and $y(G)\in\{0,1\}$ the correctness of the prediction. With
$\tau_c=0.8$, each molecule is assigned
\begin{equation}
  R(G)=
  \begin{cases}
    \textsf{conf-correct} & c\ge\tau_c,\; y=1,\\
    \textsf{conf-error}   & c\ge\tau_c,\; y=0,\\
    \textsf{borderline}   & c<\tau_c .
  \end{cases}
  \label{eq:regime}
\end{equation}
Calibration linkage is the within-cell rank correlation between per-molecule
calibration quality and reliability,
\begin{equation}
  \rho_{\mathrm{cal}} \;=\; \operatorname{Spearman}\big(1-|c-y|,\;
  \rho_{\mathrm{occ}}\big),
  \label{eq:callink}
\end{equation}
positive values supporting the hypothesis that better-calibrated predictions
carry more trustworthy attributions.

\subsection{The selection-consistency test}
\label{sec:selectiondef}

The audit's central question is operational rather than descriptive. A
practitioner with no ground truth must choose an attributor using a
faithfulness statistic; we ask whether that choice agrees with the one the
ground truth would make.

Fix a \emph{cell} $c=(\mathcal{D},f_\theta,\pi)$, meaning a dataset, a trained
backbone and a split, together with a set $\mathcal{A}$ of attributors, each audited on
the same molecule sample $\mathcal{G}_c$. For attributor $A$ write
$\bar m(A)=|\mathcal{G}_c|^{-1}\sum_{G\in\mathcal{G}_c} m_A(G)$ for the cell
mean of statistic $m$. Define the ground-truth-optimal and the
faithfulness-optimal attributor
\begin{equation}
  A^{\star} = \arg\max_{A\in\mathcal{A}} \overline{\mathrm{GT}}(A),
  \qquad
  \hat A_m = \arg\max_{A\in\mathcal{A}} \bar m(A),
  \label{eq:argmax}
\end{equation}
for each faithfulness statistic $m\in\{\rho_{\mathrm{occ}},\mathrm{Fid}^{+},
\mathrm{char}\}$. Two quantities follow. The \emph{selection gap} is the paired
per-molecule difference in correctness between the two choices,
\begin{equation}
  \delta_m(G) = \mathrm{GT}_{A^{\star}}(G)-\mathrm{GT}_{\hat A_m}(G),
  \qquad G\in\mathcal{G}_c ,
  \label{eq:gap}
\end{equation}
tested against $H_0:\operatorname{median}\delta_m=0$ with a two-sided Wilcoxon
signed-rank test on the molecules both attributors audited. The
\emph{selection-consistency coefficient} is the rank correlation between the
two orderings over $\mathcal{A}$,
\begin{equation}
  \rho_{m}^{\mathrm{sel}} \;=\;
  \operatorname{Spearman}_{A\in\mathcal{A}}\!\big(\bar m(A),\,
  \overline{\mathrm{GT}}(A)\big) \in[-1,1].
  \label{eq:selrho}
\end{equation}
$\rho^{\mathrm{sel}}\!\approx\!1$ means faithfulness is a sound surrogate for
correctness in that cell; $\rho^{\mathrm{sel}}\!\approx\!0$ means it is
uninformative; $\rho^{\mathrm{sel}}<0$ means it is actively misleading, in that
ranking by faithfulness does worse than ranking at random. Because
$\mathcal{D}$, $f_\theta$ and $\mathcal{A}$ are held fixed and only $\pi$
varies between the two arms of each comparison, any change in
$\rho^{\mathrm{sel}}$ is attributable to the split rather than to a change of
dataset or model.

\subsection{Estimation and inference}

Cell means are reported with $95\%$ percentile bootstrap intervals over
molecules ($2000$ resamples, fixed seed). Attributor comparisons within a cell
are paired on molecule identity and tested with the two-sided Wilcoxon
signed-rank test; correlations are Spearman throughout, as all statistics here
are ordinal in nature and several are heavy-tailed. Sample sizes are the
audited molecules per cell (20--120), which is small enough that we treat
individual $p$-values as descriptive and report them unadjusted for
multiplicity, leaning on replication across cells rather than on any single
test. Where a statistic is undefined for a molecule it is dropped from that
statistic's mean only, and the retained $n$ is reported alongside.

\subsection{Implementation}

The pipeline is staged and idempotent: each stage writes a marker keyed to a
content hash of its configuration, every cell is isolated so one failure cannot
abort a sweep, and trained checkpoints are content-addressed so an audit
reproduces without retraining. Seeds, library versions, git revision and
hardware are recorded per run. Attributors are the canonical
implementations~\cite{captum,pyg}, wrapped behind one interface rather than
reimplemented.

\section{Experimental setup}
\label{sec:setup}

\paragraph{Datasets} Tier-1 provides the ground-truth anchor:
\textbf{SynthMotifs} and its larger twin \textbf{SynthMotifsXL}, generated
offline (Barab\'asi--Albert base plus house/cycle motifs) with \emph{exact}
node ground truth, and \textbf{MUTAG}~\cite{mutag,tudataset} with the nitro
proxy. Tier-2 is MoleculeNet~\cite{moleculenet}: \textbf{BBBP} and \textbf{BACE}
(classification), \textbf{ESOL}, \textbf{FreeSolv} and \textbf{Lipophilicity}
(regression). Tier-3 covers chemistry-meets-medicine endpoints:
\textbf{ClinTox}, \textbf{SIDER} and \textbf{Tox21} as single-task binary views
of the MoleculeNet panels, and \textbf{DILI} (drug-induced liver injury) and
\textbf{hERG} (cardiotoxicity) via the Therapeutics Data Commons~\cite{tdc}.
TDC sets are featurised with the same PyG encoding as MoleculeNet so the
backbones are drop-in. BA-2Motifs and ShapeGGen~\cite{graphxai} are attempted
and gracefully skipped: the former's download returns HTTP 403 in this
environment, the latter's install depends on the pre-2.5 PyG compiled
extensions. Credential-gated resources are out of scope and never worked
around.

\paragraph{Backbones} GINE~\cite{gin,pretraingnn}, GCN~\cite{gcn},
GAT~\cite{gat}, MPNN~\cite{mpnn} and AttentiveFP~\cite{attentivefp}, sharing a
pooling head, calibration and splits.

\paragraph{Attributors} Integrated Gradients, Saliency, Guided Backpropagation
and Input×Gradient through Captum~\cite{captum}; GNNExplainer and PGExplainer
through PyTorch Geometric~\cite{pyg}. SubgraphX~\cite{subgraphx} requires DIG,
which installs but cannot import in this environment (no
\texttt{torch\_\-sparse} wheel at the pinned torch version), so it is left
blocked rather than approximated. PGExplainer is classification-only by
construction: its mask network trains against class logits, so it is absent
from the regression cells.

\paragraph{Splits} The primary split is a deterministic, class-aware
Bemis--Murcko scaffold split~\cite{murcko}, the protocol used to evaluate
molecular property models under realistic generalisation~\cite{mjepa} (whole scaffold groups move together,
so there is no leakage, but every fold is guaranteed to contain every class),
with a random split as the in-distribution reference. Imbalanced classification
uses inverse-frequency class weighting and AUC-based model selection. Every
comparison in this paper that is labelled ``shift'' versus ``in-distribution''
is these two splits on the same dataset.

\paragraph{Scale of the study} The sweep trains every (dataset, backbone)
pair on both splits and audits every (dataset, backbone, attributor, split)
combination, giving \nDone{} audited combinations and \nRecords{} per-molecule
audit records in total. Audited samples are 20--120 molecules per combination,
drawn from the held-out test fold. Exact software versions, hardware, seed and
the per-combination outcome ledger are recorded with the released artifacts
(\nameref{sec:availability}).

\input{generated/tab_ledger}

\paragraph{Coverage} Of the \nAttempted{} planned combinations, \nDone{}
completed and \nSkipped{} could not be attempted because a dependency
(ShapeGGen) is unavailable in this environment (Table~\ref{tab:ledger}).

A further \nCarried{} rows in the results matrix come from an earlier,
reduced-budget run on different hardware: they cover attributor sweeps on the
Tier-2/3 molecular sets that the primary configuration does not schedule, plus
the SynthMotifsXL arm. These are genuine measurements, but they are not
comparable in budget to the rest, so they are marked \textsuperscript{c}
throughout, excluded from every aggregate computed over per-molecule records,
and excluded entirely from the analysis in \nameref{sec:selection}. In total
the matrix holds \nClsRows{} classification and \nRegRows{} regression rows,
of which \nGtCells{} carry a node-level ground truth.

\section{Results}

\input{generated/tab_regression}
\input{generated/tab_paired}
\input{generated/tab_selection}
\input{generated/tab_tier1}
\input{generated/tab_molecular_1}
\input{generated/tab_molecular_2}

\subsection{Faithfulness is not correctness}

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{figs/fig_faith_correct.pdf}
\caption{\textbf{The two axes come apart, and for most cells only one of them
exists.} \textbf{(a)} Every committed cell carrying a node-level ground truth
($n=\nGtCells{}$), plotted as occlusion faithfulness against ground-truth
localisation; colour distinguishes exact synthetic ground truth from the MUTAG
motif proxy, marker distinguishes attributor, and open markers are rows carried
from an earlier run. The shaded band is GT AUROC $<0.5$: attributions there are
anti-aligned with the true motif. \nFaithfulButWrong{} cells sit in the
lower-right region, positively faithful to the model and anti-aligned with
the truth. \textbf{(b)} The remaining \nNoGtCells{} combinations, on real
molecular datasets, have no published per-atom labels: only the horizontal axis
is measurable for them.}
\label{fig:faithcorrect}
\end{figure*}

Table~\ref{tab:tier1} lists every committed cell in which attribution
\emph{correctness} can be measured at all, and Figure~\ref{fig:faithcorrect}
plots the joint distribution. Of the \nGtCells{} cells with both metrics
defined, \nAntiAligned{} are anti-aligned with the ground truth (GT AUROC
$<0.5$) and \nFaithfulButWrong{} of those are simultaneously positively
faithful to the model. That combination is the failure mode a faithfulness-only
audit cannot see, because from the model's point of view nothing is wrong.

The clearest molecular instance is the MUTAG scaffold block of
Table~\ref{tab:tier1}, where dataset, backbone and split are fixed and only the
attributor varies. Guided Backpropagation, Saliency and Input×Gradient are the
three \emph{most} faithful attributors on that cell
($\rho=\mutShiftGBPOcc{}$, $\mutShiftSalOcc{}$, $\mutShiftIxGOcc{}$) and the
three \emph{worst} at recovering the nitro motif (GT AUROC
$\mutShiftGBPGt{}$, $\mutShiftSalGt{}$, $\mutShiftIxGGt{}$). GNNExplainer,
which the ground truth ranks best at $\mutShiftGNNEGt{}$, is less faithful
($\mutShiftGNNEOcc{}$) than all three. On this cell the two axes are not merely
uncorrelated; they are inverted.

\begin{figure*}[t]
\centering
\includegraphics[width=0.495\textwidth]{figs/fig_molgrid_MUTAG.pdf}\hfill
\includegraphics[width=0.495\textwidth]{figs/fig_molgrid_SynthMotifs.pdf}
\caption{\textbf{What the disagreement looks like on the structures
themselves.} Attribution intensity overlaid on the audited graphs for four
attributors on the same molecules, rendered by the audit run: RDKit skeletal
structures for MUTAG (left) and node-link diagrams for the synthetic set
(right), with the ground-truth motif outlined in green. The quantitative gap of
Table~\ref{tab:selection} is visible here as a qualitative one: on the
synthetic graphs the gradient attributors concentrate on the outlined motif
while GNNExplainer spreads mass across the Barab\'asi--Albert base, and on
MUTAG the gradient methods saturate the fused aromatic system rather than the
nitro groups that carry the mutagenicity label. These panels are the
pipeline's own output, reproduced unmodified.}
\label{fig:molgrid}
\end{figure*}

\subsection{A fidelity-only ranking picks the wrong attributor under shift}
\label{sec:selection}

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{figs/fig_dissociation.pdf}
\caption{\textbf{Under scaffold shift a faithfulness-only ranking selects the
wrong attributor; in distribution it mostly does not.} Ground-truth
localisation per attributor, with bootstrap 95\% confidence intervals over
molecules. Within each row the dataset, backbone and attributor set are fixed
and only the split changes. \textbf{(a)} MUTAG (motif-proxy ground truth):
in distribution two of three metrics select the ground-truth-best attributor;
under shift all three select \mutShiftPickOccspearman{}, the worst-localising
of the six, significantly so (marked~*). \textbf{(b)} \synDataset{} (exact
node ground truth): the same direction with a smaller penalty. In
distribution the mismatched picks are statistically indistinguishable from the
best (marked~$\dagger$); under shift they are not. Rank correlations between
each faithfulness metric and ground truth are given below each panel.}
\label{fig:dissociation}
\end{figure*}

If faithfulness and correctness dissociate, then ranking attributors by a
faithfulness metric, which is what an evaluation framework must do when no
ground truth is available, can select the wrong method. Two cell families let us
test this cleanly: each has a node-level ground truth, all
$\mutShiftNAttr{}$ attributors audited on the same molecules, and both splits
from this run, so the in-distribution and shift arms differ \emph{only} in the
split (Table~\ref{tab:selection}, Figure~\ref{fig:dissociation}).

\paragraph{MUTAG, under shift} The ground truth ranks \mutShiftGtBest{} best
($\mutShiftGtBestVal{}$). All $\mutShiftNMetrics{}$ metrics instead rank
\mutShiftPickOccspearman{} first, at $\mutShiftPickGtOccspearman{}$, the
worst of the $\mutShiftNAttr{}$. On paired per-molecule GT AUROC over the
$\mutShiftNPairedOccspearman{}$ shared molecules the median gap is
$\mutShiftGapOccspearman{}$ with $p\mutShiftPOccspearman{}$, and
$q\mutShiftQOccspearman{}$ after controlling the false discovery rate over all
$\nSelectionTests{}$ selection tests, so this contrast is not an artefact of
testing many of them. The
faithfulness--truth rank correlation is negative for every metric:
$\mutShiftRhoOccspearman{}$ for occlusion $\rho$,
$\mutShiftRhoFidelityplus{}$ for Fidelity+ and
$\mutShiftRhoCharacterization{}$ for characterisation. A framework ranking on
faithfulness here fails to find the best attributor and systematically
prefers the worst.

\paragraph{MUTAG, in distribution} The same experiment on the random split
behaves differently. Occlusion $\rho$ and Fidelity+ both select
\mutIndPickOccspearman{}, which is also the ground-truth-best, and their rank
correlation with truth is $+\mutIndRhoOccspearman{}$. The characterisation
score is the exception, selecting \mutIndPickCharacterization{} at
$\mutIndPickGtCharacterization{}$ ($p\mutIndPCharacterization{}$) with rank
correlation $\mutIndRhoCharacterization{}$. The sign flip between the two arms
is large ($+\mutIndRhoOccspearman{}$ to $\mutShiftRhoOccspearman{}$ for
occlusion $\rho$), though this does not make in-distribution evaluation safe,
only much less dangerous.

\paragraph{The exact-ground-truth arm agrees, more mildly} On \synDataset{},
where node labels are exact rather than a motif proxy, the same pattern appears
with a smaller penalty. In distribution, occlusion $\rho$ picks the
ground-truth-best and the two field-standard metrics pick a neighbour whose GT
AUROC is statistically indistinguishable from it
($\synIndPickGtFidelityplus{}$ vs $\synIndPickGtOccspearman{}$,
$p=\synIndPFidelityplus{}$). Under shift all three select
\synShiftPickOccspearman{} ($\synShiftPickGtOccspearman{}$) over the
ground-truth-best \synShiftGtBest{} ($\synShiftGtBestVal{}$), significantly so
($p\synShiftPOccspearman{}$), though here the rank correlations stay positive
($\synShiftRhoOccspearman{}$ to $\synShiftRhoCharacterization{}$). The
direction replicates across an exact and a proxy ground truth; the severity
does not.

\paragraph{Two caveats} First, our own faithfulness metric fails alongside the
field-standard ones on the MUTAG shift arm. We therefore do not claim that
MolSanity's occlusion measure is a better selector. Under shift, \emph{no}
faithfulness metric in this experiment carries reliable information about which
attributor is correct, which argues for measuring correctness rather than for
building a better proxy. Second, $\nDegenerateGtCells{}$
of the ground-truth cells are degenerate in a way that matters here: every
audited molecule returns an identical GT AUROC of $\degenVal{}$, including
\degenCell{} ($n=\degenN{}$). That cell is the one the in-distribution MUTAG
arm names as ground-truth-best, so the ``no mismatch'' verdict there rests on a
cell with zero between-molecule variance and should not be read as a positive
result. The shift arm, which carries the argument, does not involve it.

\subsection{Reliability is a property of the cell}

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{figs/fig_heatmaps.pdf}
\caption{\textbf{Reliability is a property of the (attributor, dataset) pair,
not of the attributor.} All committed GINE cells at the scaffold split.
\textbf{(a)} Occlusion faithfulness over every audited dataset; blue $=$
anti-faithful. \textbf{(b)} Ground-truth localisation, which exists for two
datasets only. Panel~b is short because correctness is unmeasurable elsewhere,
not because it scores badly there: no molecular dataset in the sweep
publishes per-atom labels. Blank cells were not audited; a thin outline marks a
value carried from an earlier run.}
\label{fig:heatmaps}
\end{figure*}

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figs/fig_backbone.pdf}
\caption{\textbf{The backbone ordering does not survive the change of split.}
Integrated Gradients held fixed; ground-truth localisation across the
\bbIndN{} backbones on \bbDataset{}, in distribution \textbf{(a)} and under
scaffold shift \textbf{(b)}. Dashed line is chance. Spearman correlation
between the two orderings is $\bbRankRho{}$ over $\bbRankN{}$ backbones.}
\label{fig:backbone}
\end{figure}

Figure~\ref{fig:heatmaps} lays the two axes over the audited grid, and two
further comparisons show that neither axis is a stable property of the
attributor or of the architecture.

\paragraph{Backbones} Holding the attributor fixed at Integrated Gradients and
varying the backbone (Figure~\ref{fig:backbone}) gives a spread of
$\bbIndSpread{}$ AUROC in distribution (\bbIndBest{} $\bbIndBestVal{}$ down to
\bbIndWorst{} $\bbIndWorstVal{}$) and $\bbShiftSpread{}$ under shift
(\bbShiftBest{} $\bbShiftBestVal{}$ down to \bbShiftWorst{}
$\bbShiftWorstVal{}$), comparable to the spread across attributors on the
same dataset. The two orderings are negatively correlated
($\rho=\bbRankRho{}$, $\bbRankN{}$ backbones): the architecture that localises
the motif best in distribution is not the one that does so under shift. The
attributor ordering is somewhat more robust across the same change of split
($\rho=\armAttrRankRho{}$ over $\armAttrRankN{}$ attributors) but far from
preserved.

\paragraph{The shift itself does not lower faithfulness} It would be tidy if
scaffold shift simply made attributions less faithful. It does not. Across the
$\nSplitPairs{}$ cells this run audited on both splits, occlusion faithfulness
falls under the scaffold split in $\nSplitOccDrop{}$ of them, roughly half, with
a median change of $\medSplitOccDelta{}$ (Figure~\ref{fig:distributions}b).
We reported the opposite trend in an earlier, partial version of this matrix;
with the grid complete it does not hold. What shift damages is the
\emph{relationship} between faithfulness and correctness, not the level of
faithfulness, which is why a faithfulness metric gives no warning that anything
has changed.

\subsection{Where attributions fail: regimes and calibration}

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{figs/fig_distributions.pdf}
\caption{\textbf{Distributions across all audited combinations.} \textbf{(a)}
Occlusion faithfulness by dataset regime; bars are medians. \textbf{(b)} The
same cell on both splits, joined: faithfulness falls under the scaffold split
in only $\nSplitOccDrop{}$ of $\nSplitPairs{}$ cells (median change
$\medSplitOccDelta{}$), so shift does not systematically reduce faithfulness.
\textbf{(c)} Cross-checkpoint
stability by attributor; the mask-learning methods are not uniformly more
stable than the gradient ones.}
\label{fig:distributions}
\end{figure*}

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{figs/fig_regime.pdf}
\caption{\textbf{Regime stratification and calibration linkage, over the
\nRecords{} per-molecule audit records.} \textbf{(a)} Occlusion
faithfulness and \textbf{(b)} ground-truth localisation by
confidence/correctness regime; $n$ is printed per bar because ground truth
exists for only a minority of molecules. Localisation on confident-\emph{error}
molecules falls below chance ($\regCEGt{}$, $n=\nRegCEGt{}$) while occlusion
faithfulness on the same molecules is unremarkable. \textbf{(c)}
Calibration linkage computed per cell (solid $=p<0.05$): the median cell is
positive, but pooling all molecules into one correlation reverses the sign, a
Simpson's paradox and a caution against reporting the pooled number.}
\label{fig:regime}
\end{figure*}

With per-molecule records available, two audit axes that
previous drafts could only define are now measurable
(Figure~\ref{fig:regime}).

\paragraph{Regime stratification} Pooling the \nRegimeMol{} audited molecules
by regime gives the audit's most directly actionable result. Ground-truth
localisation is highest on confident-correct molecules ($\regCCGt{}$,
$n=\nRegCCGt{}$), drops to $\regBLGt{}$ on borderline ones
($n=\nRegBLGt{}$), and falls \emph{below chance}, to $\regCEGt{}$, on
\emph{confident-error} molecules ($n=\nRegCEGt{}$). When the model is
confidently wrong, its attribution points away from the substructure that
matters rather than towards it. That is the regime a deployment cares most
about and the one in which an explanation is most likely to be believed.
Occlusion faithfulness gives no warning: it is $\regCEOcc{}$ on those same
confident-error molecules, no worse than the $\regCCOcc{}$ of the
confident-correct ones. The confident-error ground-truth estimate rests on
$\nRegCEGt{}$ molecules and is reported with that $n$ throughout; it is
suggestive rather than precise.

\paragraph{Calibration linkage} The framework's hypothesis is that
better-calibrated predictions carry more trustworthy attributions. Computed per
cell it is supported on balance (median $\rho=\medCalibRho{}$ across
$\nCalibCells{}$ classification cells, $\nCalibSigPos{}$ significantly positive
against $\nCalibSigNeg{}$ significantly negative), but the effect is
cell-specific rather than universal. Pooling every molecule into a single
correlation instead yields $\pooledCalibRho{}$: the opposite sign. The pooled
number is an artefact of mixing cells with different confidence distributions,
and we report it only to warn against it.

\subsection{Cross-task reliability}
\label{sec:crosstask}

Tables~\ref{tab:regression}, \ref{tab:molecular} and~\ref{tab:molecularb} give
every audited combination on a real molecular dataset. Three cross-task
patterns hold, and one apparent pattern turns out to be an artefact.

\paragraph{The regression faithfulness operator was mis-specified, and we
withdraw its numbers} Across the \nRegCells{} regression combinations,
\nRegNeg{} have negative occlusion faithfulness (median $\medRegOcc{}$), on
cells whose predictive performance is well behaved ($R^2$ from
$\esolRtwoMin{}$ to $\esolRtwoMax{}$ on ESOL). We do not interpret that
pattern, and on inspection it is an artefact of our own operator rather than a
property of the attributions. The operator compared two different quantities:
the attribution was clipped at zero, keeping only atoms that push the
prediction \emph{up}, while the occlusion effect kept its sign. A motif that
drives a solubility prediction strongly down therefore scored as unimportant on
the attribution side and dominant on the occlusion side, which is sufficient on
its own to produce a systematic negative correlation. The unbounded
output-space Fidelity$\pm$ values, which reach $\maxRegFidPlus{}$ against the
$[-1,1]$ a probability-space fidelity occupies, are the visible symptom of the
same mismatch. The corrected operator ranks by magnitude on both sides, reports
the GraphFramEx characterisation score as undefined rather than clipping a
standard-deviation-space shift into $[0,1]$, and adds a bounded statistic
giving the share of the total occlusion effect carried by the salient atoms. It
is implemented and tested but postdates this sweep, so Table~\ref{tab:regression}
is retained for completeness and the regression cells are excluded from every
faithfulness claim in this paper. The classification path, on which every other
result rests, is unchanged.

\paragraph{Coherence is high nearly everywhere, and says little} Motif top-1
share lies between $\motifTopMin{}$ and $\motifTopMax{}$ across the
\nMolecularCells{} molecular cells, with \nMotifInBand{} of them inside a
narrow $0.6$--$0.9$ band, largely independent of whether the same cell is
faithful or anti-faithful. Concentration on a single motif is easy to achieve
and, on this evidence, close to uninformative about reliability. It belongs in
the audit as a descriptive statistic, not as a quality score.

\paragraph{Stability is high but not uniform} Median cross-checkpoint stability
over \nStabilityCells{} cells is $\medStability{}$, with a minimum of
$\minStability{}$, a cell whose early and final attributions are essentially
uncorrelated. By attributor, GNNExplainer is the most stable
(median $\stabGNNE{}$) and Integrated Gradients sits at $\stabIG{}$ over
$\nStabIG{}$ cells; the per-attributor counts are very unequal in this run, so
these medians are descriptive rather than a controlled comparison.

Table~\ref{tab:paired} gives the paired attributor contrasts behind
Section~\nameref{sec:selection}, computed from the per-molecule records.

\subsection{Negative and degenerate results}

We record what did not work alongside what did.

\paragraph{Ground truth that was not being read} BA-2Motifs is a synthetic
benchmark whose whole purpose is to carry exact node labels. It trains in this
run and produces $2\times100$ audited molecules, but both its cells sit in the
no-ground-truth block, because the loader found no node labels to read. The
cause was ours rather than the dataset's: PyG's \texttt{BA2MotifDataset}
exposes only node features, an edge index and a graph-level label, and our
extractor looked for a per-node field that is never populated. The labels are
recoverable from the released node ordering, in which the injected motif is
appended after the Barab\'asi--Albert base. That recovery is now implemented and
verified structurally, by requiring the induced subgraph on those nodes to be a
house or a five-cycle before the mask is used, and cross-checked against a
generator that produces the same construction with independent labels. It is
refused rather than guessed when the check fails, so a reordered loader
degrades to the behaviour reported here. The numbers in this paper predate that
fix: BA-2Motifs is counted as audited and not as a ground-truth cell throughout,
and the third exact-ground-truth arm it will provide is not claimed here.

\paragraph{Degenerate ground truth} $\nDegenerateGtCells{}$ ground-truth cells
return the identical value $\degenVal{}$ on every audited molecule, including
\degenCell{} ($n=\degenN{}$). A cell with zero between-molecule variance
cannot support a confidence interval or a paired test, and one of the two is
the attributor the in-distribution MUTAG arm names as ground-truth-best. We
report those cells with the rest and mark what rests on them.

\paragraph{The best model has the worst attributions} The highest-AUC molecular
classification cell in the run is \bestAucDataset{}·\bestAucBackbone{} under the
scaffold split, at accuracy $\bestAucAcc{}$, ROC-AUC $\bestAucAuc{}$ and ECE
$\bestAucEce{}$, so accurate and well calibrated. Its attributions are the most
\emph{anti}-faithful of any molecular cell in the matrix
($\rho=\bestAucOcc{}$): Integrated Gradients ranks the motifs of these
molecules in close to the reverse of their causal order. Predictive quality,
calibration and explanation reliability are three different axes.

\paragraph{A weak model can be faithfully explained} The converse also appears.
The most faithful molecular cell is
\mostFaithfulDataset{}·\mostFaithfulBackbone{} ($\rho=\mostFaithfulOcc{}$) on a
model with accuracy $\mostFaithfulAcc{}$ and ROC-AUC $\mostFaithfulAuc{}$,
barely better than chance. Faithfulness certifies that the explanation
describes the model; it says nothing about whether the model is worth
describing. Any reliability report that omits the predictive metrics can be
satisfied by this cell.

\paragraph{Accuracy is the wrong headline on imbalanced sets}
\nHighAccRows{} committed rows report test accuracy $\geq0.95$, with ROC-AUC as
low as $\minHighAccAuc{}$. On Tox21, accuracy runs $\toxAccMin{}$--$\toxAccMax{}$
against AUC $\toxAucMin{}$--$\toxAucMax{}$. We report AUC alongside accuracy
throughout for this reason, and the lowest-AUC cell in the run
(\worstAucDataset{}·\worstAucBackbone{}, AUC $\worstAucAuc{}$ at accuracy
$\worstAucAcc{}$) is exactly the case where accuracy alone would mislead.

\paragraph{Blocked, and re-examined} The results reported here come from a run
in which SubgraphX (via DIG) and ShapeGGen (via GraphXAI) were both recorded as
uninstallable, on the grounds that they need the compiled
\texttt{torch\_\-sparse}/\texttt{torch\_\-scatter} extensions for which no wheel
exists at the pinned torch version. Revisiting that diagnosis, it was wrong in
both cases. Those extensions build from source, DIG then installs, and
SubgraphX is now wrapped and covered by tests in which it recovers a planted
five-node motif exactly; it is enabled in the configuration and will populate
the matrix on the next sweep. GraphXAI installs from a source checkout rather
than a release, and ShapeGGen builds. It remains unintegrated for a different
and more substantial reason: ShapeGGen is a node-classification benchmark on a
single large graph, whereas every axis of this audit is defined per molecule at
the graph level, so adopting it means extending the audit to node-level tasks
rather than resolving a dependency. Both are recorded in the ledger, occupy no
row in any table, and enter no aggregate. We report the earlier diagnosis
alongside the correction because a coverage gap attributed to the environment
turned out to be a gap in how hard we looked.

\section{Discussion}

\paragraph{What the audit buys} The single most useful thing MolSanity produces
is not a score but a \emph{negative certificate}. For \nNoGtCells{} of the
\nRowsTotal{} committed rows, covering every real molecular dataset in the
sweep, attribution correctness cannot be measured at all, because no per-atom
explanation labels exist. Those cells can be audited for faithfulness,
coherence, stability and calibration, and this paper's central result is that
under shift none of those is a reliable stand-in for correctness. Making that
gap explicit is more useful than filling it with a metric that looks like
correctness and is not.

\paragraph{How attributions should be reported} Four practices follow directly
from the matrix. (i) Report the regime: an attributor selected in distribution
carries no warrant out of it, and on the one cell family where we can check,
the selection is significantly wrong under shift. (ii) Report the backbone: the
backbone ordering does not survive the change of split ($\rho=\bbRankRho{}$),
so ``IG on a GNN'' is under-specified. (iii) Report the predictive metrics next
to the explanation metrics, because the accurate, well-calibrated
\bestAucDataset{}·\bestAucBackbone{} cell has the most anti-faithful
attributions in the matrix. (iv) Do not pool: the calibration-linkage
correlation changes sign between the per-cell and the pooled analysis.

\paragraph{Why reliability is regime-, backbone- and dataset-dependent} A
faithfulness metric asks about the model; a ground-truth metric asks about the
chemistry; they agree only when the model has learned the chemistry. That
coincidence is engineered into synthetic benchmarks and is not guaranteed on
molecular data, particularly out of distribution, which is the situation Faber
et al.~\cite{faber} identify as making ground-truth comparison hazardous in
general. Attribution evaluation inherits the model's inductive limitations, so
the correct object of study is the cell, (dataset, backbone, attributor,
split), and not the attributor alone.

\section{Limitations and threats to validity}
\label{sec:limits}

\paragraph{Ground truth is scarce, and one arm of it is a proxy} The two cell
families that carry the argument have $n=\mutShiftNMol{}$ audited molecules per
arm. MUTAG, the molecular one, uses a chemically motivated nitro/aromatic-nitro
SMARTS proxy rather than annotator labels: it encodes an assumption about which
substructure ought to matter, and the trained model may use a different,
equally valid rationale~\cite{faber}. \synDataset{}, where the labels are
exact, is a structural non-molecular graph task. The inversion replicates
across both, which is the strongest available evidence, but neither arm is
simultaneously molecular and exactly labelled. No public dataset is, which is
the gap this paper is about.

\paragraph{Degenerate ground-truth cells} $\nDegenerateGtCells{}$ cells return
an identical GT AUROC on every audited molecule, including \degenCell{}
($\degenVal{}$, $n=\degenN{}$). Zero between-molecule variance means the
bootstrap interval is uninformative and any ``best attributor'' verdict resting
on such a cell is fragile; we flag them rather than drop them, and the shift
arms that carry the argument do not involve them.

\paragraph{A dataset that trains but was not scored} BA-2Motifs loads and
trains in this run, but its cells contribute faithfulness only and appear in
the no-ground-truth block despite being a synthetic benchmark designed to have
exact labels, because our extractor did not read the ordering its node labels
are encoded in. The recovery is implemented and tested but postdates this
sweep, so the third exact-ground-truth arm is available to the next run and is
not part of any number reported here.

\paragraph{Single split, single seed} Each cell is one deterministic split at
one seed. Cross-checkpoint stability partially probes run-to-run variability,
but no confidence interval in this paper covers split or seed variance, and the
per-cell molecule counts (20 on the synthetic arms) make individual contrasts
underpowered.

\paragraph{Occlusion as a counterfactual} Zeroing node features takes the graph
off the data manifold; a motif whose removal barely moves the output may be
redundant rather than unimportant. This bears most heavily on the regression
cells, where the output-space Fidelity$\pm$ values leave the range a
probability-space fidelity occupies and we decline to interpret the sign.

\paragraph{Proxy ground truth} MUTAG's mask is a nitro/aromatic-nitro SMARTS
proxy, not annotator labels. It encodes an assumption about which substructure
ought to matter, and the trained model may use a different, equally valid
rationale~\cite{faber}.

\paragraph{Coverage of attributors} SubgraphX contributes no row to this run
and PGExplainer failed in it, so every conclusion about the perturbation family
rests on GNNExplainer alone in the current-run cells. SubgraphX is wrapped and
tested but its cells postdate this sweep, so the gap stands for the numbers
reported here.

\paragraph{Multiplicity} The selection tests carry Benjamini--Hochberg
adjusted $q$ values over the family of $\nSelectionTests{}$ tests, and the
attributor contrasts carry them over the family within each cell block, so the
headline mismatches are false-discovery-rate controlled rather than descriptive.
What the adjustment cannot repair is power: at $n=\mutShiftNMol{}$ molecules per
arm a contrast that fails to reach significance is weak evidence of no
difference, not evidence of none.

\section{Conclusion}

Molecular GNN attributions are audited today for whether they describe the
model. Across the committed audit matrix we find that this is not the same
question as whether they describe the chemistry, and that the two diverge in
the regime molecular property prediction actually operates in. On the one cell
family where exact ground truth, several attributors and both splits coexist, a
faithfulness-only ranking is harmless in distribution and selects a
significantly worse attributor under scaffold shift. MolSanity's own
faithfulness metric is no exception. Reliability does not transfer across
backbones or across splits, and for the \nNoGtCells{} molecular rows in the
matrix, correctness cannot be measured at all with the labels the field
currently has. The audit is what makes those failures visible; the pipeline,
the figures and every number here regenerate from the committed artifacts as
the grid fills in.

\section{Data and code availability}
\label{sec:availability}

All code, configurations, trained checkpoints, per-combination results,
per-molecule audit records and the run manifest (seed, library versions,
hardware, and the per-combination outcome ledger) are available at
\url{https://github.com/Kar488/molsanity}, released under the MIT licence. A
versioned archive of the release, including the trained weights, is deposited
with a DOI \emph{[DOI to be inserted on deposition]}; the repository carries
\texttt{CITATION.cff} and \texttt{.zenodo.json} so the archive is generated
from a tagged release.

Every figure and table in this paper is generated from those artifacts by
\texttt{paper/figs/make\_\-figures.py} and
\texttt{paper/figs/make\_\-tables.py}, which read the released results
directly; no value in this manuscript is transcribed by hand. Re-running them
after a new sweep updates every number, figure and table in place. Dataset
licences and provenance are recorded in the repository's dataset manifest; the
credential-gated resources referenced in Section~\nameref{sec:setup} are out of
scope and were never accessed.

\clearpage
\begin{thebibliography}{99}
\footnotesize
\setlength{\itemsep}{1.5pt}\setlength{\parskip}{0pt}

\bibitem{ig} M.~Sundararajan, A.~Taly, and Q.~Yan. Axiomatic attribution for
deep networks. \emph{Proceedings of the 34th International Conference on
Machine Learning (ICML)}, PMLR 70:3319--3328, 2017.

\bibitem{saliency} K.~Simonyan, A.~Vedaldi, and A.~Zisserman. Deep inside
convolutional networks: visualising image classification models and saliency
maps. \emph{International Conference on Learning Representations (ICLR)
Workshop}, 2014.

\bibitem{guidedbp} J.~T.~Springenberg, A.~Dosovitskiy, T.~Brox, and
M.~Riedmiller. Striving for simplicity: the all convolutional net.
\emph{International Conference on Learning Representations (ICLR) Workshop},
2015.

\bibitem{gnnexplainer} Z.~Ying, D.~Bourgeois, J.~You, M.~Zitnik, and
J.~Leskovec. GNNExplainer: generating explanations for graph neural networks.
\emph{Advances in Neural Information Processing Systems (NeurIPS)}, 2019.

\bibitem{pgexplainer} D.~Luo, W.~Cheng, D.~Xu, W.~Yu, B.~Zong, H.~Chen, and
X.~Zhang. Parameterized explainer for graph neural network. \emph{Advances in
Neural Information Processing Systems (NeurIPS)}, 2020.

\bibitem{subgraphx} H.~Yuan, H.~Yu, J.~Wang, K.~Li, and S.~Ji. On
explainability of graph neural networks via subgraph explorations.
\emph{Proceedings of the 38th International Conference on Machine Learning
(ICML)}, PMLR 139:12241--12252, 2021.

\bibitem{gradcamgraph} P.~E.~Pope, S.~Kolouri, M.~Rostami, C.~E.~Martin, and
H.~Hoffmann. Explainability methods for graph convolutional neural networks.
\emph{IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR)},
2019.

\bibitem{gnnsurvey} H.~Yuan, H.~Yu, S.~Gui, and S.~Ji. Explainability in graph
neural networks: a taxonomic survey. \emph{IEEE Transactions on Pattern
Analysis and Machine Intelligence}, 45(5):5782--5799, 2023.

\bibitem{graphxai} C.~Agarwal, O.~Queen, H.~Lakkaraju, and M.~Zitnik.
Evaluating explainability for graph neural networks. \emph{Scientific Data},
10:144, 2023.

\bibitem{graphframex} K.~Amara, Z.~Ying, Z.~Zhang, Z.~Han, Y.~Zhao, Y.~Shan,
U.~Brandes, S.~Schemm, and C.~Zhang. GraphFramEx: towards systematic evaluation
of explainability methods for graph neural networks. \emph{Proceedings of the
First Learning on Graphs Conference (LoG)}, PMLR 198, 2022.

\bibitem{dig} M.~Liu, Y.~Luo, L.~Wang, Y.~Xie, H.~Yuan, S.~Gui, H.~Yu, Z.~Xu,
J.~Zhang, Y.~Liu, K.~Yan, H.~Liu, C.~Fu, B.~M.~Oztekin, X.~Zhang, and S.~Ji.
DIG: a turnkey library for diving into graph deep learning research.
\emph{Journal of Machine Learning Research}, 22(240):1--9, 2021.

\bibitem{molfaith} M.~Hiltscher, M.~Bianciotto, and F.~Grisoni. Explaining
What Matters: Faithfulness in Molecular Deep Learning. \emph{ChemRxiv}, 2026.
\href{https://doi.org/10.26434/chemrxiv.10001594/v1}
{doi:10.26434/chemrxiv.10001594/v1}. Platform: \url{https://github.com/molML/MolFaith}.

\bibitem{faber} L.~Faber, A.~K.~Moghaddam, and R.~Wattenhofer. When comparing
to ground truth is wrong: on evaluating GNN explanation methods.
\emph{Proceedings of the 27th ACM SIGKDD Conference on Knowledge Discovery and
Data Mining (KDD)}, pages 332--341, 2021.

\bibitem{sanchezlengeling} B.~Sanchez-Lengeling, J.~N.~Wei, B.~K.~Lee,
E.~Reif, P.~Wang, W.~W.~Qian, K.~McCloskey, L.~J.~Colwell, and
A.~B.~Wiltschko. Evaluating attribution for graph neural networks.
\emph{Advances in Neural Information Processing Systems (NeurIPS)}, 2020.

\bibitem{sanitychecks} J.~Adebayo, J.~Gilmer, M.~Muelly, I.~Goodfellow,
M.~Hardt, and B.~Kim. Sanity checks for saliency maps. \emph{Advances in Neural
Information Processing Systems (NeurIPS)}, 2018.

\bibitem{roar} S.~Hooker, D.~Erhan, P.-J.~Kindermans, and B.~Kim. A benchmark
for interpretability methods in deep neural networks. \emph{Advances in Neural
Information Processing Systems (NeurIPS)}, 2019.

\bibitem{captum} N.~Kokhlikyan, V.~Miglani, M.~Martin, E.~Wang, B.~Alsallakh,
J.~Reynolds, A.~Melnikov, N.~Kliushkina, C.~Araya, S.~Yan, and O.~Reblitz-Richardson.
Captum: a unified and generic model interpretability library for PyTorch.
\emph{arXiv:2009.07896}, 2020.

\bibitem{pyg} M.~Fey and J.~E.~Lenssen. Fast graph representation learning with
PyTorch Geometric. \emph{ICLR Workshop on Representation Learning on Graphs and
Manifolds}, 2019.

\bibitem{rdkit} G.~Landrum et al. RDKit: open-source cheminformatics.
\url{https://www.rdkit.org}.

\bibitem{gin} K.~Xu, W.~Hu, J.~Leskovec, and S.~Jegelka. How powerful are graph
neural networks? \emph{International Conference on Learning Representations
(ICLR)}, 2019.

\bibitem{pretraingnn} W.~Hu, B.~Liu, J.~Gomes, M.~Zitnik, P.~Liang, V.~Pande,
and J.~Leskovec. Strategies for pre-training graph neural networks.
\emph{International Conference on Learning Representations (ICLR)}, 2020.

\bibitem{gcn} T.~N.~Kipf and M.~Welling. Semi-supervised classification with
graph convolutional networks. \emph{International Conference on Learning
Representations (ICLR)}, 2017.

\bibitem{gat} P.~Veli\v{c}kovi\'c, G.~Cucurull, A.~Casanova, A.~Romero,
P.~Li\`o, and Y.~Bengio. Graph attention networks. \emph{International
Conference on Learning Representations (ICLR)}, 2018.

\bibitem{mpnn} J.~Gilmer, S.~S.~Schoenholz, P.~F.~Riley, O.~Vinyals, and
G.~E.~Dahl. Neural message passing for quantum chemistry. \emph{Proceedings of
the 34th International Conference on Machine Learning (ICML)}, PMLR
70:1263--1272, 2017.

\bibitem{attentivefp} Z.~Xiong, D.~Wang, X.~Liu, F.~Zhong, X.~Wan, X.~Li,
Z.~Li, X.~Luo, K.~Chen, H.~Jiang, and M.~Zheng. Pushing the boundaries of
molecular representation for drug discovery with the graph attention mechanism.
\emph{Journal of Medicinal Chemistry}, 63(16):8749--8760, 2020.

\bibitem{mjepa} K.~Iyer and N.~Sabar. M-JEPA: predictive self-supervised
learning for molecular graphs with scaffold-shift evaluation on Tox21.
\emph{Journal of Chemical Information and Modeling}, 66(14):8008--8021, 2026.
\href{https://doi.org/10.1021/acs.jcim.6c00828}{doi:10.1021/acs.jcim.6c00828}.

\bibitem{moleculenet} Z.~Wu, B.~Ramsundar, E.~N.~Feinberg, J.~Gomes,
C.~Geniesse, A.~S.~Pappu, K.~Leswing, and V.~Pande. MoleculeNet: a benchmark
for molecular machine learning. \emph{Chemical Science}, 9(2):513--530, 2018.

\bibitem{tdc} K.~Huang, T.~Fu, W.~Gao, Y.~Zhao, Y.~Roohani, J.~Leskovec,
C.~W.~Coley, C.~Xiao, J.~Sun, and M.~Zitnik. Therapeutics Data Commons:
machine learning datasets and tasks for drug discovery and development.
\emph{NeurIPS Datasets and Benchmarks Track}, 2021.

\bibitem{mutag} A.~K.~Debnath, R.~L.~Lopez de Compadre, G.~Debnath,
A.~J.~Shusterman, and C.~Hansch. Structure-activity relationship of mutagenic
aromatic and heteroaromatic nitro compounds. Correlation with molecular orbital
energies and hydrophobicity. \emph{Journal of Medicinal Chemistry},
34(2):786--797, 1991.

\bibitem{tudataset} C.~Morris, N.~M.~Kriege, F.~Bause, K.~Kersting, P.~Mutzel,
and M.~Neumann. TUDataset: a collection of benchmark datasets for learning with
graphs. \emph{ICML Workshop on Graph Representation Learning and Beyond}, 2020.

\bibitem{murcko} G.~W.~Bemis and M.~A.~Murcko. The properties of known drugs.
1. Molecular frameworks. \emph{Journal of Medicinal Chemistry},
39(15):2887--2893, 1996.

\bibitem{calibration} C.~Guo, G.~Pleiss, Y.~Sun, and K.~Q.~Weinberger. On
calibration of modern neural networks. \emph{Proceedings of the 34th
International Conference on Machine Learning (ICML)}, PMLR 70:1321--1330, 2017.

\end{thebibliography}
